Context for future Codex sessions (quick start, no full tree walk needed):

- Project: Rust WASM formant/pitch analyzer with a web front-end under `webapp/`.
- Front-end now uses React + Vite. Entry: `webapp/src/main.tsx` renders `webapp/src/ui/App.tsx`; formant/pitch work stays in `webapp/src/formantWorker.ts` (module worker). WASM bindings live in `webapp/pkg/` (generated by wasm-pack; imported at runtime).
- Tooling: `webapp/package.json` scripts:
  - `npm run dev` (Vite dev server), `npm run build` (Vite build, static-copies `pkg/`), `npm run preview`, `npm run typecheck`, `npm run lint`, `npm run format`.
- Vite config: `webapp/vite.config.ts` (base "./", copies `pkg/**/*` into `dist/pkg`, outputs to `webapp/dist/`).
- Ignored: `webapp/dist/`, `webapp/node_modules/`, `.vite` (see `webapp/.gitignore`). Keep `pkg/` generated before building.
- Manual testing: ensure mic permissions are granted in the browser; spectrum/LPC/formant lines render on `Real-time Spectrum` tab, rolling history on `Formant History` tab.
